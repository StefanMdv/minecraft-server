services:
  mc-server:
    build: .
    container_name: mc-server
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
    volumes:
      - mc-data:/server
    restart: unless-stopped

volumes:
  mc-data: