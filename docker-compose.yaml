services:
  mc-server:
    build: .
    container_name: mc-server
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      MEMORY: "2G"
      JVM_OPTS: "-Xmx2G -Xms2G"
    volumes:
      - mc-data:/data
    restart: unless-stopped

volumes:
  mc-data: {}