services:
  mc-server:
    build: .
    container_name: mc-server
    ports:
      - "25565:25565"
    env_file:
      - .env
    volumes:
      - mc-data:/data
    restart: unless-stopped

volumes:
  mc-data: {}